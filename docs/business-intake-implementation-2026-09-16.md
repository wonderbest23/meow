# 사업 진단 중심 대화 전환

## 적용 범위

메인 대화에 규칙 기반 진단을 추가했다. 공개 기본값은 아직 기존 흐름이며, 새 흐름은 빌드 환경의 `NEXT_PUBLIC_BUSINESS_INTAKE_V2=1`로 연다. 운영 배포, 외부 테스트 서버 배포, 실제 유료 AI 호출은 이번 검증에서 하지 않았다.

- 아이디어 탐색 / 구상한 사업 / 운영 중인 사업의 세 진입 유형
- 핵심 질문 각각 11개, 이미 확인한 항목 건너뛰기, 미정·이전 답변 수정
- 11개 업종의 선택 상세 질문 각각 4개
- 질문 분기·직접 답변·기본 요약·재무 계산·저장에는 외부 AI 호출 없음
- 사용자 입력과 AI 사업안을 별도로 표시하며 생성은 명시적 요청에서만 실행

## 대화 화면 보완

규칙 기반 진단을 별도 설문처럼 보여주던 화면을 기존 채팅 흐름으로 되돌렸다. 기존 환영 메시지와 시작 선택지를 사용하고, 질문과 답변은 대화 안에 쌓인다. 단일 선택은 즉시 답변으로 전송하며 직접 입력과 복수 선택은 고정된 하단 입력창에서 전송한다. 기본 질문의 판단과 저장에 AI가 개입하지 않는 원칙은 유지한다.

하단 입력창은 시작 전부터 표시한다. 처음 적은 사업 설명은 대화 생성과 사업 내용 저장을 한 번의 CAS 요청으로 처리하며 시작 유형 버튼 선택을 강제하지 않는다. 이미 입력한 사업 설명은 다음 질문에서 반복하지 않는다. 명시적인 아이디어 없음/운영 중 답변은 해당 분기로 시작한다. 자유 메모와 AI 도움은 같은 입력창의 별도 모드이며, 직접 답변을 AI 도움으로 자동 전환하지 않는다. 이전 답변은 말풍선에서 수정할 수 있고 모바일은 대화/사업 요약을 전환한다.

2026-09-16 추가 검증: 전체 70/70 회귀 묶음 및 타입 검사 통과. 브라우저에서 320/390/768/1440px, 모바일 높이 692px의 입력창 고정과 가로 넘침, 세 진입 유형 완주, 말풍선 수정, 다중 탭 충돌, 응답 유실 후 재전송, 첫 메시지의 새로고침/중복 저장 방지를 확인했다. 보고서는 `artifacts/business-intake/1789528690904/report.json`이며 31회 질문 DOM 전환은 약 4~14ms였다. 유료 API 호출 및 운영 배포는 없었다.

후속 전송 오류 수정: 첫 입력 이후 `introMessage`가 남아 전송 버튼을 잠그던 조건과 901px 미만에서 Enter 전송을 막던 조건을 제거했다. Enter는 전송, Shift+Enter는 줄바꿈이며 한글 조합 중에는 전송하지 않는다. 소프트웨어/카페 등 명확히 일치하는 선택지 이름은 타이핑해도 기본 답변으로 처리한다. 기존 임시 입력과 새 입력이 같으면 한 번만 저장하고 서로 다르면 둘 다 보존한다. UI 단위 검사, 서비스 23개 사례, 타입 검사를 통과했고 실제 인앱 브라우저에서 기존 입력 전송과 새로고침, 390px에서 첫 입력/선택지 타이핑/직접 답변 전송을 확인했다. 이 후속 수정의 실제 브라우저 검증은 인앱 브라우저로 수행했으며 앞선 CLI 브라우저 보고서는 화면 복원 시점의 기록이다. 추가 유료 API 호출은 없었다.

환영 화면 정리(2026-09-16 후속): 사업이 만들어지기 전에는 상단 저장 상태 바(`입력 중 · 이 기기에 보관`, 진행도)를 표시하지 않는다. 첫 저장 이후부터 저장됨/저장 중/저장 실패/저장 충돌과 `기본 질문 N/11`을 표시한다. 시작 전 저장 실패는 기존 오류 알림이, 전송 중은 보내기 버튼 스피너가 알린다. 환영 제목 위의 장식 문구 `생각이 사업이 되는 순간`은 진단 화면에서만 끈다(`CoachWelcome tagline={false}`). 홈페이지 폰 데모와 기존 대화 화면은 그대로다.

선택형 전환 0단계(2026-09-16 후속): (1) 복수 선택 답변이 내부 값(`b2b_service, software`)으로 말풍선·요약·사업 후보 카드에 표시되던 결함을 고쳤다. `intakeValueLabel`이 배열 값도 선택지 라벨로 바꾸며(intake-core.ts), 후보 카드 근거 문구는 업종 내부 값 대신 라벨을 쓴다(intake-questions.ts). (2) 운영 중 경로의 실적 기간 질문은 타이핑 형식 지시 대신 프리셋 칩(최근 1/3/6/12개월, 올해)과 `직접 선택`(날짜 선택기 2개)으로 답한다. 선택값은 기존 서버 검증 형식 `YYYY-MM-DD / YYYY-MM-DD`로 전송되므로 intake-core.ts의 기간 검증은 그대로다. 프리셋 계산은 `periodPresetRange`(model.ts)이며 월 말일은 대상 월의 마지막 날로 맞춘다. 입력창 타이핑은 예외 경로로만 남긴다. 검증: 타입 검사, business-intake-ui/questions/core/service 단위 검사, 70/70 회귀 통과, 격리 미리보기에서 복수 선택 라벨 표시와 프리셋 저장 확인.

헤더·상태 바·탭 정리(2026-09-16 후속, 소유자 승인): 헤더 아래 "저장됨 · 기본 질문 N/11" 바와 모바일 "대화 / 사업 요약" 탭 줄을 없앴다. 진행도는 헤더 아래 3px 진행선과 헤더 부제("기본 질문 3/11", `BusinessAppChrome`의 `subtitle`)로 옮겼다. 저장 상태 요소는 하나만 두고 `role="status"`·`data-status`를 유지하되, 저장 중·저장 실패·저장 충돌·불러오는 중일 때만 화면에 보이고 저장됨·입력 중은 시각적으로만 숨긴다(`srOnly`). 모바일(≤900px)에서는 헤더의 "사업 관리" 옆에 요약 토글 버튼(`actions` 슬롯, 답변 수 배지)이 답변이나 확정 필드가 1개 이상일 때만 나타나고, 누르면 요약이 대화 자리를 대신하며 상단 "대화로 돌아가기"로 복귀한다. PC(≥901px)는 요약이 오른쪽에 그대로 있어 토글·시트 헤더가 표시되지 않는다. 새 대화 첫 화면은 헤더 한 줄과 첫 질문만 남는다. 브라우저 회귀의 탭 단언은 헤더 토글과 복귀 버튼으로 바꿨다.

업종 지도 데이터 층(2026-09-16 후속, 소유자 요청으로 공공데이터포털 자료 직접 수집): `data/ksic/`에 한국표준산업분류(KSIC 11차)·한국표준직업분류 원본 CSV 2개(공공데이터포털 15159306, 15159305, CP949 원본 그대로, SHA-256 기록)와 `scripts/build-ksic.ts`가 만든 UTF-8 색인(`ksic-2025.json` 2,038개 산업 항목: 대 21·중 77·소 234·세 501·세세 1,205, `ksic-linkage-2025.json` 10차↔11차 연계 4,478행)을 두었다. `lib/plan-builder/ksic.ts`는 계층 조회(`ksicByCode`/`ksicChildren`/`ksicAncestors`/`ksicPath`), 규칙 기반 검색(`searchKsic`: 구어 동의어 245개 + 이름 토큰, AI 0회), 중분류 77개 전부의 사업 구조 기본값(`KSIC_DIVISION_STRUCTURES`: 지불자·제공 형태·수익 방식·전달 방식·자본 형태·인허가·소규모 창업 여부·11업종 호환값)과 소분류 예외(`KSIC_STRUCTURE_OVERRIDES`), 11업종↔중분류 표(`SECTOR_DIVISIONS`)를 제공한다. 검사 `scripts/ksic.test.ts`(회귀 묶음 등록)가 개수·부모 무결성·구조 커버리지·동의어 코드 존재·표본 사업 문장 10종의 후보 1위를 고정한다. **이용 조건 주의**: 포털 표기가 공공저작물 제4유형(상업적 이용 금지·변경 금지)이라 운영 배포 전 확인이 필요하다(`data/ksic/README.md`). 아직 진단 화면·업종 추천에는 연결하지 않았다(다음 단계).

KSIC 후보를 업종 질문에 연결(2026-09-16 후속): 확인된 사업 소개·상품 텍스트에서 `searchKsic`로 세세분류 후보 최대 4개를 서버가 계산해 `snapshot.ksicCandidates`로 내려 주고(클라이언트에 170KB 색인을 싸지 않음), 업종 질문(inChat, 수정 중 아님)에 "표준산업분류 후보" 칩으로 표시한다. 칩을 누르면 `answer` 명령에 `ksic`(5자리)를 함께 보내고, 서버는 코드에서 11업종을 도출해 `intake.sector`·`coach.business.industry`를 정하며 `intake.ksic`에 코드를 저장한다(잘못된 코드는 400 `invalid_ksic`, 코드 없이 다시 답하면 null). 사용자 말풍선은 "업종: 커피 전문점 (카페 · 음식점)", 요약에는 "커피 전문점 · KSIC 56221"이 붙는다. `snapshot.ksic.structure`로 사업 구조 기본값(인허가 등)이 함께 내려온다. 저장 스키마·DB 변경 없음(`__business_intake.state.ksic` 필드 추가만). 검사: 서비스 스위트에 KSIC 답변 케이스, UI 스위트에 후보 칩 렌더 단언 추가, 71/71 회귀 통과. 격리 미리보기에서 "동네에서 작은 카페를 하고 싶어요" → 커피 전문점 칩 → 저장·요약 반영 확인.

구조 기본값 반영(2026-09-16 후속, 1단계): 확정된 KSIC의 사업 구조 기본값이 세 곳에 쓰인다. (1) 질문: 가격 질문의 기준 단위가 수익 방식에서 나온다(`revenueBasis` — 월 구독형은 "월 구독 1건", 프로젝트형은 "프로젝트 1건"; 건당 결제는 업종 기본 유지). 처리량 질문의 단위 칩 순서가 제공 형태에서 나온다(`capacityUnitOrder` — 공간은 좌석·룸, 실물은 개, 서비스는 건 우선). 질문 수·저장 형식은 그대로다. (2) 요약: 업종 줄에 지불자·제공 형태·전달·수익·인허가 라벨 5개(`structureSummary`)와 인허가 안내 한 줄(`licenseHint`, 신고/허가/자격/세부 확인)이 붙는다. 법적 판단이 아니라 업종 분류 기준의 기본 안내다. (3) 문서: `confirmedIntakeContext`가 intake 상태에서 `ksic` 코드 하나만 읽어(정규식·색인 검증) 코드·이름·경로·구조 라벨·출처를 프롬프트 컨텍스트에 넣고, 규칙 문장에 "분류 기준 기본값이며 확정 답변을 덮어쓰지 않는다"를 추가했다. 다른 intake 상태(메모·후보·작업)는 여전히 읽지 않는다. 검사: ksic/서비스(SaaS→월 구독 기준, 공유오피스→좌석·룸 우선)/컨텍스트(확정 KSIC 포함·잘못된 코드 무시·비밀 미노출) 스위트 추가.

구조 기본값 반영 2단계(2026-09-16 후속): (1) 요약의 금액·수량 항목 라벨이 수익 방식을 따른다(`structureFieldLabels` — 월 구독형은 "월 구독 가격·월 구독자 수·구독자 1명당 월 비용", 프로젝트형은 "프로젝트 1건 가격·월 프로젝트 수"; 저장 키는 그대로). (2) 시작 준비 단계(`launchSteps`)에 확정 KSIC의 인허가 기본값이 있으면 사업자등록 앞에 "업종 인허가를 확인해요" 단계가 추가된다(신고·허가·자격 안내 + 확인 메모 서식 + 정부24 링크). 개선 목적(운영 중)에서는 추가하지 않는다. (3) 업종 이름 직접 검색: `GET /api/plan/chat?ksic=검색어`(x-business-intake: 2)가 규칙 기반 세세분류 후보 6개를 돌려주고(저장 없음, AI 0회), 업종 질문의 검색 상자(300ms 디바운스)가 결과를 같은 칩으로 보여 준다. 2글자 미만은 검색하지 않고, 결과가 없으면 11개 업종 선택으로 안내한다. 검사: ksic(라벨), business-launch(인허가 단계 존재·순서·미확정 시 없음), UI(검색 상자 렌더) 추가.

KSIC 검색 품질 보정(2026-09-16 후속): 공식 이름에 흔한 일반 단어(대행·서비스·운영·판매·제조·소매·기타 등)는 이름 매칭에서 제외하고, 2글자 이하 동의어 키는 토큰 앞부분 일치로만 맞춘다("책"이 "산책"에 걸리지 않게, "출판"은 "출판사"에 맞게). 동의어 268개(심부름·펫시터·캠핑장·요가 스튜디오·중고 명품·무인 가게 등 추가). 표본: "심부름 대행" → 기타 개인·사업 지원 서비스, "캠핑장 운영" → 야영장업, "요가 스튜디오" → 기타 스포츠시설, "무인 아이스크림 가게" → 기타 가공식품 소매업. 규칙 기반 추천은 여전히 추천일 뿐이며 사용자가 확정한다.

#### 사업 구조 편집과 소분류 검수 (22:15~22:25)

사업 요약에 "사업 구조" 절을 추가했다. 고객·지불자 / 제공하는 것 / 전달 방식 / 수익 방식 / 인허가 다섯 축을 한 줄씩 보여 주고, 각 줄 옆 연필을 누르면 그 축의 값 전부가 칩으로 펼쳐진다. 칩을 누르면 `POST /api/plan/chat`에 `{ action: "structure", structure: { 축: 값 } }`가 가고, 서버가 `intake.structure`에 저장한 뒤 "사업 구조 수정: 수익 방식: 건당 결제 → 월 구독" 말풍선을 남긴다. 줄마다 출처 표시(직접 선택 / 분류 기준 / 업종 기준)가 붙고, 사용자가 고른 값은 이후 질문의 가격 기준 단위·처리량 단위·요약 라벨·문서 컨텍스트 모두에서 KSIC·업종 기본값보다 우선한다. 값 검증은 zod 스키마(축 이름·값 enum)와 `applyIntakeStructure`의 라벨 존재 확인 두 겹이고, 바꿀 축이 없으면 `structure_required`(400)다.

타입·라벨·업종별 기본값(`SECTOR_DEFAULT_STRUCTURE`)은 `lib/plan-builder/business-structure.ts`로 옮겨 클라이언트가 KSIC JSON 색인(약 2,000행)을 번들하지 않게 했다. `ksic.ts`는 이를 다시 내보내므로 기존 import는 그대로 동작한다.

소분류 검수: 별도 에이전트가 소분류 234개(세분류·세세분류 이름 대조 포함)를 중분류 기본값과 대조해 제안한 60개와, 판단이 갈린 9개 중 7개를 `KSIC_STRUCTURE_OVERRIDES`에 반영했다(총 91개 예외). 대표 정정: 호프·생맥주·소주방은 유흥주점 허가가 아닌 일반음식점 신고, 옷가게·가전·문구·가구 소매는 인허가 없음, 정육점·반찬가게는 신고, 안경원은 안경사 면허, 볼링장은 게임당·당구장은 시간당(체력단련장 월 회비 상속 오류), 광고·컨설팅·시장조사·행사대행은 자격 불필요, 세차업은 정비업 등록 대상 아님, 주류 제조는 제조면허, 종합건설업은 허가가 아닌 등록. 유지한 것: 편의점(세부 확인), 포털(없음). 각 예외의 근거는 코드 주석에 한 줄씩 있다. **이 값들은 법령 조문 대조가 아닌 상식 수준 검수이므로 운영 전 전문가 확인이 남아 있고, 화면 문구는 계속 "~한 편입니다, 관할 기관 기준으로 확인"이다.**

검사: 서비스 스위트(구조 명령 저장·출처 `user`·가격 기준 추종·말풍선·잘못된 값 거부·KSIC 없을 때 업종 기본값), UI 스위트(구조 절 렌더), 컨텍스트 스위트(사용자 구조값이 문서 프롬프트에 반영, 잘못된 값 무시), ksic 스위트(예외 91개의 코드 존재·값 유효성, 정정 10건 표본). tsc 통과, 진단 스위트 전부 통과, 71/71 회귀 통과. 격리 서버에서 카페(56221) → 요약 → 수익 방식 연필 → 월 구독 칩 → "직접 선택" 표시와 말풍선을 직접 확인했다.

#### 구조 기준 질문·계산 분기 (22:40~23:10)

기본 진단(AI 0회)은 창업 모드에서 변동비·고정비를 묻지 않아 손익 계산이 한 번도 돌지 않았다("가격·건당 변동비·월 고정비가 모두 정해진 뒤 계산합니다"가 영구 표시). 이번에 상세 팩 앞에 구조(수익 방식) 질문을 붙여 이를 채운다. 구성은 모델별 지표 1개(월 구독 → 평균 유지 기간, 대여·이용 → 이용률, 수수료 → 수수료율, 프로젝트 → 문의→계약 기간, 시간당 → 주당 청구 시간; 건당·복합은 없음) → 변동비(수익 단위에 맞춘 문구·라벨, 0원 칩 포함 사다리) → 월 고정비(운영 중은 기본 질문에 있어 제외). 전부 선택형·선택 사항이고 "상세 질문 N개 추가" 버튼 숫자가 팩 크기를 따른다(카페·월 구독 = 3 + 4 = 7).

손익 문장은 `intakeFinancialReference(coach, intake)`가 만든다. 가격·변동비·고정비가 모두 있어야 계산하고, 없으면 어떤 값이 비었는지 이름을 적는다. 월 판매량은 volume 필드 → 처리량 환산(하루 ×26일, 일주일 ×4.3주, 한 달 ×1; 기간을 안 고르면 환산하지 않음) → 주당 청구 시간 ×4.3 순으로 잡고, 대여·이용 모델은 이용률을 곱한다. 모델별 추가 지표: 구독은 생애 매출·월 이탈률·유지에 필요한 월 신규 구독자, 수수료는 평균 거래액, 프로젝트는 첫 입금 시점. 환산 가정은 문장에 그대로 적고 성장률은 0으로 둔다. 요약 "금액과 운영 수치"와 문서 생성(artifact-update-service의 financialReference)이 같은 문장을 쓴다.

저장 규칙: 변동비·고정비는 coach 필드(unitCost·cost)가 원천이라 intake/details에 중복 저장하지 않는다. 모델별 지표는 intake/details에 들어가고 문맥·문서 라벨은 `allStructureQuestions()`로 찾는다. 수익 방식을 고치면 팩이 바뀌고 비활성이 된 답(예: 이용률)은 질문 기록에만 남는다.

검사: questions 스위트(모델별 팩 구성, 운영 모드 고정비 제외, 프리셋·사다리 보장, 구조 없이도 라벨 조회), core 스위트(행렬 33조합이 11 + 팩 크기, 필드 질문은 coach.fields로), service 스위트(SaaS 끝까지: 손익분기 월 38건, 생애 매출 360,000원, 월 이탈률 약 8%, 매달 신규 약 5명; 대여 이용률 70% 저장·170% 거부; 수익 방식 수정 시 팩 교체), context 스위트(구조 답 라벨), 71/71 회귀. 격리 서버에서 카페(월 구독으로 수정)에 기본 11개를 채운 뒤 "상세 질문 7개 추가" → 12개월 → 0원 → 상한 150만원까지 클릭만으로 진행해 요약에 손익분기 월 50건·생애 매출 360,000원·매달 신규 약 9명이 뜨는 것을 확인했다.

#### 탐색 모드에 업종 지도 연결 (2026-09-17 12:20~13:00)

"무슨 사업을 해야 할지" 경로는 고정 템플릿 30개 중 3개만 보여 줬다. 이제 표준산업분류 세세분류 1,205개가 후보 공간이 된다. 탐색 기본 질문에 "시작 조건"(다중 선택, 최대 4개, 선택 사항)을 사업 후보 앞에 넣었고, 칩 9개(무점포로 시작 / 혼자 시작할 수 있는 일 / 인허가 없이 시작 / 온라인으로 제공 / 방문·출장으로 제공 / 매장·공간에서 제공 / 개인 고객 / 기업·사업자 고객 / 월 구독·정기 수익)는 사업 구조 축(자본·소상공인·인허가·전달·고객·수익)에 그대로 대응한다. 저장값은 칩 라벨이다(선택형 규칙). 탐색 기본 질문은 12개가 됐다.

후보 생성은 `ksicCandidateIdeas(intake, coach)`(서버, AI 0회). 관심 업종·경험·조건 중 하나도 없으면 비어 있고 템플릿 3개만 남는다. 있으면 세세분류 중 구조값의 업종이 관심 업종에 들고, 소상공인 창업 후보이며(smallBusiness), 고른 조건을 전부 만족하는 것만 남긴 뒤 경험 문장 일치(`searchKsic`) → 구어 표현이 있는 흔한 창업 업종 → 코드 순으로 최대 5개를 고른다. 카드에는 코드·소분류 이름·구조 라벨 5개, 근거(경험 입력의 "네일"과 이어지는 분류 / 관심 분야 / 조건 일치), 확인할 점(인허가 안내, 수요·수익성 미검증)이 붙는다. 템플릿 3개가 먼저, 지도 후보가 뒤에 온다.

지도 후보를 고르면 공식 이름이 사업 소개·사업명이 되고 `intake.ksic`에 코드가 저장돼 업종·구조 기본값(가격 기준, 처리량 단위, 인허가 단계)이 바로 따라온다. 후보를 "아직 미정"으로 되돌리면 코드도 지운다. 잘못된 후보 id는 `invalid_option`이다.

검사: questions(조건 질문 구성·라벨 저장·선택 상한 4), core(행렬 33조합이 모드별 기본 질문 수를 따름, 전부 미정도 완료 처리), service(신호 없음 → 템플릿 3개만; 생활 서비스 + "네일아트 자격증" → 96119가 지도 후보 첫 자리에 근거 "네일"과 함께; 소프트웨어 + 무점포·인허가 없음 → 후보 전부 자본 remote·인허가 none; 선택 시 코드·업종·구조 출처 ksic; 되돌리면 코드 삭제; AI 호출 0), 71/71 회귀. 격리 서버에서 생활·지역 서비스 → 미용·뷰티·건강·운동 → 20시간 → 300만원 → 개인 고객 → 후보 8개(템플릿 3 + 두발 미용업·피부 미용업·기타 미용업·일반 전기 공사업·부동산 중개 및 대리업) → 기타 미용업 선택 → 코드 96119·사업명·구조 출처 저장을 확인했다.

한계: 경험 일치가 없을 때 채우는 후보는 구어 표현 유무와 코드 순이라 관심 업종 안에서 임의성이 남는다(위 예의 전기 공사업·부동산 중개). 관심 업종을 고르지 않고 조건만 고르면 전 업종에서 찾는다. 조건을 고르고도 지도 후보가 0개면 후보 질문에 "고른 시작 조건을 모두 만족하는 업종이 지도에 없어요. 시작 조건에서 한두 개를 빼면 후보가 늘어요."를 붙인다(13:10 추가, 서비스 스위트에 모순 조건 검사).

#### 문서 템플릿의 구조 기준 절 (2026-09-17 13:05~13:25)

문서(사업계획서) 생성·갱신·결과물 변경은 모두 AI 프롬프트에 `intakeContext`(확정 답변 JSON)를 넘긴다. 여기에 `structure` 블록을 추가했다. 만드는 곳은 `lib/plan-builder/intake-structure-brief.ts`의 `intakeStructureBrief(coach, intake)`이고 분류 기본값 + 사용자 구조 선택 + 확정 입력만으로 결정적으로 채운다(추정치 없음). 절은 세 개다. (1) 수익 모델: 수익 방식, 가격 기준 단위, 산식(예: 월 매출 = 월 구독 가격 × 월 구독자 수; 생애 매출 = 가격 × 유지 개월), 확정 입력(가격·변동비·고정비·처리량), 모델 지표(유지 기간·이용률·수수료율·문의→계약 기간·청구 시간). (2) 인허가 체크리스트: 분류 기준 절차(인허가 없음/신고·등록/허가/자격·면허/세부 확인)별 안내 문장, 확인 항목 5개(기관·절차 이름, 서류·비용·기간, 시설·위생 기준, 사업자등록 업종 코드 반영, 확인 날짜 기록), 사용자가 상세 질문에서 고른 인허가·인증 상태(공간 이용 조건, 품질·시험 확인). (3) 초기 자본: 자본 형태(매장·설비·차량·무점포·복합)별 항목 목록, 준비 예산·월 고정비 입력, 예비 운전자금 예시(산식 명시: 월 고정비 × 3). 여기에 손익 시나리오 문장(`intakeFinancialReference`)과 규칙 4줄(기본값은 추정·사용자 선택 우선, 인허가는 확인 필요로, 입력 금액만 사용·나머지는 추가 정의 필요, 시나리오 밖 매출·이익 계산 금지)이 붙는다.

포함 조건: 분류 코드가 있거나, 사용자가 구조를 직접 골랐거나, 손익 입력이 하나라도 있을 때만. 그 밖에는 추측이 되므로 넣지 않고 기존 빈 문맥 규칙을 유지한다. 크기 제한(8,000자)을 넘으면 시나리오 문장 → 구조 블록 → KSIC → 업종 → 실적 기간 → 상세 답변 순으로 뺀다. 완전한 진단 상태가 없는 계획(예: 코드만 확정된 오래된 기록)은 확정된 코드·업종·구조 선택만으로 최소 상태를 만들어 브리프를 낸다. 문서 갱신(`document-refresh-service`)의 `financialReference`도 진단이 있으면 수익 방식·처리량을 반영한 문장을 쓰도록 바꿨다(결과물 변경 관리는 이미 그렇게 함).

검사: 컨텍스트 스위트(코드 확정 시 브리프 존재·출처 문구·인허가 상태·산식·자본 항목·규칙), 서비스 스위트(SaaS 끝까지: 월 구독 산식·가격 기준·입력·유지 기간 지표·인허가 없음·무점포·예비 운전자금 3,000,000원·손익분기 문장). 문서 본문을 실제로 생성해 보지는 않았다(유료 AI). 프롬프트 지시문은 그대로이며 자료(JSON)만 늘었으므로, 첫 실제 생성 때 세 절이 문서에 어떻게 반영되는지 사람이 확인해야 한다.

#### 표본 사업 10종 완주 테스트 (2026-09-17 13:10~13:30)

`scripts/business-intake-coverage.test.ts`(회귀 묶음 72번째). 네일샵·이사업체·반찬가게·온라인 영어과외·굿즈 스마트스토어·소규모 SaaS·촬영 스튜디오·공유오피스·퀵배송·부품 제조 10종을 창업 모드로 시작해 기본 11개 + 상세 팩(구조 2~3 + 업종 4)을 끝까지 답한다. 답은 전부 화면이 주는 것만 쓴다: 업종은 사업 문장에서 나온 KSIC 후보 칩(기대 코드와 일치해야 함), 단일·다중은 첫 선택지, 금액은 사다리의 양수 하한(변동비는 0원 칩), 숫자는 프리셋, 문장형은 그룹별 첫 칩을 이어 붙인 문장(처리량은 인력 / 기간 N단위). 칩·프리셋·사다리가 없는 질문이 하나라도 나오면 실패. 끝난 뒤 코드 저장, 구조 라벨 5개, 손익분기 문장, 요약 11행 이상, 네트워크·AI 호출 0을 확인한다. 결과 10/10. 부품 가공은 동의어가 없어 "산업용 가스 제조업"이 잡혔었고 부품가공·부품·금속가공·절삭·도금·용접 → 25924·25929(절삭 가공, 기타 금속 가공) 동의어를 추가했다.

#### 미분류·복합 사업 fallback, 지도 채움 후보 순서, 실제 문서 생성 확인 (2026-09-17 13:35~14:00)

**미분류·복합 fallback.** 구조 기본값을 그대로 믿기 어려운 두 경우를 서버가 표시한다(`snapshot.structure.fallback`). 복합: 사업 소개에 이음말(와·과·및·겸·하면서·함께 등)이 있고 분류 검색의 강한 일치(구어 표현 또는 이름 점수 24 이상)가 두 업종 이상에 걸칠 때. 미분류: 업종을 '새로운 사업·미분류'로 두었고 분류 코드도 없을 때(창업 모드는 업종 답변 뒤부터). 기본값은 바꾸지 않는다. 화면은 업종 단계에 "매출이 가장 큰 쪽을 먼저 고르면 나머지는 사업 구조에서 복합으로 표시"를, 요약의 사업 구조 절에 "다섯 축을 직접 고르면 질문과 계산이 그 구조를 따라간다"(미분류) 또는 "주 업종 기준, 섞인 축은 복합"(복합)을 보여 준다. 문서 브리프의 출처 문장에도 같은 표시가 붙는다. 검사: 서비스 스위트("카페와 도자기 공방" → 복합, 주 코드 선택 뒤에도 유지·기본값 불변, 이음말 없는 SaaS는 이름 토큰 잡음이 있어도 복합 아님, 미분류는 업종 답변 뒤에만, 사용자 축 선택이 가격 기준을 바꿈). 격리 서버에서 복합 문장을 직접 입력해 두 안내가 뜨는 것을 확인했다.

**지도 채움 후보.** 경험과 이어지지 않는 채움 후보를 코드 순으로 늘어놓던 것을, 그 업종을 부르는 구어 표현 수(동의어 사전)를 흔한 창업 업종의 대리 지표로 써서 정렬한다. 경험 일치가 있으면 채움은 2개까지만 붙고, 구어 표현이 있는 업종이 하나도 없을 때만 나머지 분류로 채운다. 모든 지도 후보 카드에 "흔히 부르는 말: 네일, 왁싱…"을 붙여 낯선 공식 이름(기타 미용업)을 풀어 준다. 남은 한계: 구어 표현 수는 사전 작성량에 좌우되는 대리 지표이고 실제 창업 빈도 통계가 아니다.

**실제 문서 생성 확인(유료, 1회).** `scripts/structure-brief-live.mts`를 만들었다. 기본은 드라이런(네트워크·키·장부 쓰기 없음)으로 가상 사업을 진단으로 만들고 운영 경로와 같은 생성 입력을 조립해 요청 본문에 세 절이 실렸는지와 보수적 예약 상한을 계산한다. `--live --approved-total-usd=30`일 때만 `.env.local`의 OpenAI 키 하나를 읽고 기존 누적 장부(`SyntheticAiBudget`)로 선예약하며 호출은 2회(생성 + 검토)로 막는다. 가상 사업 "동네 원두 정기 구독 카페"(KSIC 56221, 수익 방식을 월 구독으로 수정, 가격 30,000원·변동비 9,000원·고정비 150만원·예산 1,000만원)로 `funding/requirements`(자금 소요) 1개 항목을 생성했다. 결과: source ai, 2회 호출(입력 5,168/4,224 · 출력 1,721/76 토큰), 예약 $2.95355. 본문에 브리프의 자본 항목(보증금·선납 임차료, 인테리어·간판·설비, 초도 재료, 신고·홍보, 예비 운전자금 "제공된 3개월분 예시 450만원"), 인허가 확인 문구("신고·시설 요건과 비용: 관할 시·군·구청 위생 담당 부서에 확인"), 월 30,000원 커피 패스가 반영됐고, 입력·산식에 없는 금액이나 법령·기관 단정은 없었다. 증거: `artifacts/structure-brief-live/2026-09-17T04-44-22-234Z/result.json`(git 제외). 문서가 브리프의 숫자를 인용해도 품질 검사에서 없는 숫자로 판정되지 않도록 `intakeContextEvidence`에 브리프의 확정 입력·지표·산식·손익 문장을 근거로 포함시켰다(서비스 스위트에 검사). 확인 범위는 1개 항목·1개 업종이다. 수익 모델 절이 주로 쓰일 `financials/revenue`·`strategy/price` 항목은 생성해 보지 않았다.

#### PC 분할 조절, 다음 단계 강조, AI 작업 진행 게이지 (2026-09-17 14:40~15:20)

소유자 피드백(운영 전환 직후): PC에서 채팅이 끝나면 오른쪽의 "사업안 만들기"가 헷갈린다, AI 제작 중에는 돌아가는 아이콘뿐이라 얼마나 걸리는지와 화면을 나가도 되는지 알 수 없다.

**PC 분할.** `useChatSplit`에 옵션(저장 키, 기본 비율, 최소 px, 최대 비율)을 넣어 새 진단이 같이 쓴다(예전 화면은 기본값 그대로). 대화 | 12px 손잡이 | 요약의 3열 그리드이고 비율은 `--chat-share`. 질문에 답하는 동안은 대화 64%, 기본 질문이 끝나면 대화 42%·요약 58%로 자동 전환된다(0.45초 전환, 모션 줄이기 설정이면 즉시). 손잡이는 마우스 드래그, 키보드(←→ 2%씩, Home/End), 두 번 눌러 기본값 복귀를 지원하고 단계별로 너비를 따로 기억한다(localStorage, 실패해도 기본값으로 동작). 900px 이하는 기존 단일 열 + 헤더 요약 토글 그대로다.

**다음 단계 강조.** 지금 눌러야 할 버튼 하나에만 파란 2px 테두리, "다음 단계" 배지(화살표 아이콘), 한 줄 안내가 붙는다. 순서는 기본 질문 완료 → 사업안 만들기 → (사업안이 현재 입력 기준이면) 계획서 만들기 → 결과물 열기. 기본 질문이 끝나면 요약의 버튼 묶음이 요약 맨 위로 올라오고(긴 요약을 끝까지 내려야 버튼이 보이던 문제), 대화의 마지막 정리 화면에도 같은 다음 단계 버튼이 생긴다. 끝나기 전에는 아래쪽에 비활성 버튼과 "기본 질문을 마치면 다음 단계가 열려요"가 보인다.

**진행 게이지.** 작업 기록에 요청 시각(`createdAt`)을 남기고, API 응답을 보낼 때마다 서버 시각 기준 경과 시간과 종류별 보통·최대 시간(`jobClock`: 사업안 25초/60초, 도움말·메모 정리 8초/20초 — 최대는 실제 호출 제한 시간과 같다)을 붙인다. 화면은 그 값에서 이어 세므로 기기 시계 오차와 무관하고, 화면을 나갔다 와도 같은 지점에서 이어진다. 퍼센트는 추정치다(실제 신호는 접수·실행·완료뿐). 보통 시간에 80%, 이후 천천히 95%까지만 오르고 완료 응답이 와야 끝난다. 표시는 "AI 사업안 작성 중 43% · 예상 진행률 · 8초 경과 · 보통 25초 안팎", 오래 걸리면 "평소보다 오래 걸리고 있어요(최대 60초)", 그리고 "이 화면을 닫거나 다른 페이지로 가도 서버에서 계속 진행돼요. 돌아오면 결과가 여기에 표시돼요." 게이지는 방금 누른 다음 단계 버튼 자리(대화)와 요약의 버튼 아래 두 곳에 나온다.

**소유자 재피드백 반영(15:00~15:20).** 버튼이 너무 많고 같은 버튼이 두 곳에 있어 헷갈린다, 사업안과 계획서 버튼이 왜 둘 다 있느냐, PC에서 마우스 조절이 전혀 안 된다. (1) 다음 단계는 한 번에 하나, 한 곳에만: `intakeNextStep`이 사업안 만들기 → (사업안이 현재 입력 기준이면) 계획서 만들기 → 계획서 열기 중 하나만 돌려주고 `NextStepAction`이 그 하나만 그린다. 사업안(방향 요약, 20~30초)과 계획서(전체 문서, 몇 분)는 이어지는 두 단계라 동시에 보여 주지 않는다. 답변을 고쳐 사업안이 옛 기준이 되면 다시 사업안 만들기로 돌아온다. (2) 위치도 한 곳: 대화의 마지막 정리 화면이 보이면 그 안에만 두고 요약에는 버튼을 두지 않는다(`showActions=false`). 아직 답할 질문이 남아 정리 화면이 없을 때만 요약 맨 위에 둔다. (3) 정리 화면 구성: 모바일은 위에 작은 "답변 수정하기"(요약 열기), 그 아래 한 상자에 [사업안 만들기][상세 질문 N개 추가]를 한 줄로. PC는 같은 상자이고 요약이 옆에 있으므로 "답변 수정하기"는 숨긴다. (4) 마우스 조절이 안 된 원인은 좁은 PC 폭에서 조절 범위가 49~54%로 묶였던 것(최소 폭 360/340px). 최소 폭을 300/300px, 최대 75%로 풀어 같은 폭에서 39~61%가 됐고 손잡이를 14px·더 진한 색으로 키웠다. 실제 마우스 드래그는 브라우저 회귀가 확인한다.

화면을 나가도 유지되는 근거: 운영에서는 `dispatchIntakeJob`이 Cloudflare Workflow(`intake-<jobId>`)를 만들어 서버에서 실행하고 화면은 2.5초마다 상태만 확인한다. 로컬·데모 저장소에서는 응답 뒤 같은 서버 프로세스가 이어서 실행한다(서버가 꺼지면 사라진다). 120초 동안 상태가 바뀌지 않은 작업은 다음 조회 때 실패로 정리된다.

검사: 서비스 스위트(사업안 작업이 요청 시각을 남기고, 화면의 상태 확인 없이 서버 실행만으로 끝나며, 돌아와 읽으면 결과가 저장돼 있음; 서버 기준 경과 시간 계산, 예전 기록 호환), UI 스위트(게이지 수식: 단조 증가·95% 상한·완료 100%, 게이지 마크업과 안내 문구, 다음 단계 표시가 단계마다 정확히 한 곳), 브라우저 회귀(1440px에서 실제 마우스로 손잡이를 끌어 대화 너비가 줄고 저장되며 두 번 눌러 복귀, 가로 넘침 없음). 격리 서버에서 분할 64% → 42% 자동 전환, 키보드 조절과 단계별 저장, 다음 단계 테두리 2곳, 게이지 두 곳 표시를 직접 확인했다. 격리 서버는 유료 AI가 꺼져 있어 게이지는 브라우저 안에서 응답만 흉내 내어 확인했다.

#### 구조값 인허가 법령 검수 반영 (2026-09-17 13:35~14:05)

검수 에이전트가 중분류 기본값 77행과 예외 91행(168행)의 인허가 축을 법령 조문과 대조했다(조문은 국가법령정보센터 2026-09-17 기준 확인, 미확인은 "조문 미확인" 표시). 결과는 유지 142, 변경 25, 미확정 1(08 광업 지원 서비스업). 전문은 `docs/ksic-license-review-2026-09-17.md`에 보존했다.

반영한 변경. 중분류 15개: 01 농업·02 임업·16 목재·27 의료정밀·30 자동차·31 운송장비·50 수상운송·59 영상·60 방송·94 단체 → 세부 확인, 19 석유정제·39 환경정화·61 통신 → 신고·등록(허가제 아님), 28 전기장비·45 자동차 판매 → 인허가 없음. 예외 11개: 9611 이용·미용(네일·피부 포함) → 자격·면허(공중위생관리법 제8조: 면허 없이 개설 불가), 96122 마사지 → 자격·면허(의료법 제82조), 9021 독서실·스터디카페 → 세부 확인(스터디카페는 등록 대상 아님 판결), 857 교육 지원 → 세부 확인(입시컨설팅은 학원 등록 대상), 639·63120 → 세부 확인, 494 택배 → 허가(화물자동차 운송사업 허가가 전제, 49402 퀵은 없음 유지), 91134 볼링장 → 없음, 91231 낚시장 → 세부 확인, 95213 세차 → 신고·등록(폐수배출시설 설치신고), 4512 중고차 → 등록 명시. 새 예외 11개: 91139 요가·필라테스 없음, 96991 예식장 없음, 271 의료기기 허가, 47222 건강기능식품 신고, 63992 가상자산 신고 + 소규모 창업 아님, 9111·9112·59141·55101·55103 소규모 창업 아님, 35114 태양력 발전 소규모 창업 가능. 예외는 총 102개가 됐다. 주석의 구법 표기(주세법 → 주류 면허 등에 관한 법률, 유원시설업 → 테마파크업)도 고쳤다. 자격·면허 안내 문장에 "개설 신고·등록 절차도 확인"을 넣었다.

검사: ksic 스위트에 검수 표본 14건(미용 면허, 안마 자격, 요가 자유업, 볼링장·당구장, 자동차 판매·중고차, 교육 지원, 통신 등록, 의료기기, 택배·퀵, 가상자산·태양력), 서비스 스위트의 네일 후보 설명을 "자격·면허 필요"로 갱신. 한계: 신뢰도 MED·LOW로 표시된 16개(31, 02, 16, 27, 30, 50, 60, 94, 494, 9021, 63120, 95213, 96122, 6620, 85502, 56219)는 인허가 전문가 확인 대상이고, 화면 문구는 계속 "~한 편입니다, 관할 기관 기준으로 확인"을 유지한다.

### 질문 전환과 스크롤 후속 보완

사용자 답변을 즉시 말풍선으로 표시한 뒤 점 세 개의 `다음 질문 준비 중` 상태를 거쳐 다음 질문을 보여준다. 600ms는 대화 표현을 위한 최소 간격이며 저장 요청을 늦추거나 AI를 호출하지 않는다. 저장 실패·충돌은 대기를 바로 해제하고 입력을 보존한다. 계정·경로 변경과 화면 종료에서는 이전 전환 타이머를 취소하며 모션 감소 설정에서는 추가 대기와 진입 애니메이션을 생략한다.

자동 스크롤의 기준을 다음 질문에서 마지막 사용자 답변으로 바꿨다. 짧은 답변은 다음 질문과 함께 보이고, 긴 답변은 끝부분을 유지한다. 사용자가 이전 대화를 읽으려고 스크롤하면 강제로 따라가지 않는다. 화면 크기 변경에도 위치를 다시 계산한다. 현재 질문의 뒤로가기 버튼은 제거하고 `아직 미정`을 명확한 보조 버튼으로 표시한다. 이전 답변 목록과 미복구 입력은 사업 요약으로 옮겼으며 말풍선의 개별 수정은 유지한다.

최신 브라우저 보고서: `artifacts/business-intake/1789530883270/report.json`. API 키 없는 격리 서버에서 세 진입 유형 완주, 320/390/526/768/1440px 답변·질문 동시 노출, 저장 충돌, 응답 유실 후 재접속·중복 방지, 첫 입력 전송과 모션 감소를 통과했다. 31개 답변의 사용자 말풍선 DOM 반영은 약 9~22ms, 다음 질문은 약 607~629ms였다. 앞선 보고서의 300ms 기준은 즉시 사용자 피드백과 규칙 계산에 적용하며 다음 질문의 연출 간격과 구분한다. 실제 인앱 브라우저에서도 선택 답변과 직접 입력 모두 점 세 개 표시와 말풍선 유지가 확인됐다. 유료 API 호출 및 운영 배포는 하지 않았다.

이 보완 이후 TypeScript 검사와 전체 신뢰성 테스트 70/70 묶음을 다시 통과했다. 사용자가 열어 둔 기존 사업의 답변은 변경하지 않고 별도 가상 사업으로 브라우저 입력을 검증했다.

### 사업 설명 기반 업종 추천

업종 질문에서 확인된 사업 소개·제공 상품의 문구를 사용해 추천 업종을 먼저 보여준다. `50만원으로 SNS컨설팅`은 기업 서비스로 추천하며, `이 업종으로 계속`을 눌러야 기존 answer API로 확정한다. `직접 선택하기`에서는 기존 11개 업종을 펼쳐 선택한다. 기존 업종 수정과 복구된 선택은 전체 목록을 사용하고 추천으로 덮어쓰지 않는다.

기존 제안서 업종 분류 규칙을 재사용하고 제공 형태와 고객 업종이 다른 경우를 일부 보완했다. 확정되지 않은 AI 제안·자유 메모는 추천 근거에서 제외한다. 단서가 없거나 부정 표현·복합 사업·상충하는 입력이면 수동 선택으로 남긴다. 규칙 기반 추천이므로 의미를 완벽하게 판별한다고 보장하지 않으며 확인 전 공통 사업정보와 결과물은 변경하지 않는다. 추가 AI 호출은 없다.

업종별 예시·미분류·고객 업종 구별·부정·복합 사업 등 18개 분류 사례와 확정 전 무변경·수동 선택 보존 검사를 UI 단위 테스트에 추가했다. TypeScript 검사 및 브라우저 회귀 검사가 통과했다. 최신 보고서 `artifacts/business-intake/1789531580929/report.json`에 320/390/526/768/1440px 화면, 추천 확인, 직접 선택, 재접속, 기존 업종 수정, 중복 클릭과 모호한 입력 검증을 기록했다. 유료 API 호출 0회이며 운영에는 배포하지 않았다.

## 저장과 AI 경계

공통 사업정보는 기존 coach 필드와 일반 answers에 둔다. `__business_intake`는 질문팩 버전, 원문, 질문 상태, 후보, 작업 상태, 요청 영수증을 보관한다. 업종을 바꾸면 이전 상세 답변은 질문 기록에 남기고 현재 문서 근거에서는 제외한다.

자유 메모는 원문부터 저장한다. 명확한 항목 라벨은 규칙으로 후보를 만들고, 복합 문장은 별도 추출 작업에 전달한다. 후보는 원문 근거가 일치해야 하며 사용자 승인 전 공통 정보에 반영하지 않는다. 값 비교와 항목별 입력 이력 비교를 함께 사용해 변경 후 원래 값으로 돌아온 경우도 재확인한다.

추출은 사업당 동시 1작업, 외부 호출 1회, 20초, 최대 1,200 출력 토큰이다. 자동 재시도나 제공자 폴백은 없다. 새로 대기한 메모만 다음 작업으로 묶으며 실패한 메모의 재시도는 명시적으로 요청한다. 로컬 브라우저를 닫으면 후속 대기 메모는 저장 상태로 남고 재접속 때 이어진다. 실행 상태 확인이 120초를 넘기면 미완료로 복구하며 원문은 보존한다.

저장 요청 제한과 AI 요청 제한을 분리했다. 실제 AI 실행 전 서버 공통 카운터를 확인하며 조회 실패 시 외부 호출하지 않는다. 기본 진단은 AI 한도가 소진돼도 계속된다.

대화/질문 상태, 진단 작업 상태, 문서 근거 버전을 분리했다. 동일 답변을 다시 저장하거나 메모·로딩 상태가 바뀌었다는 이유로 문서가 오래된 것으로 표시되지 않는다. 기존 문서·PPT·홈페이지의 승인형 갱신 경로를 유지한다.

문서 생성은 원자적 예약과 요청 영수증으로 중복 Workflow를 막는다. 완료된 같은 요청은 기존 실행을 반환한다. 비동기 해석 상태는 독립 증가 버전으로 보호해 늦게 끝난 문서 저장이 후보나 메모 상태를 되돌리지 못한다.

## 로컬 검증

- 전체 회귀 테스트 70/70 묶음과 TypeScript 검사 통과
- 핵심 진단: 33개 업종·진입 조합 포함 123개 검사, 외부 fetch 0회
- 서비스 21개: AI 키 없음, 429, 잔액 부족, 잘못된 JSON, 지연 응답, 중복 요청, 수동 수정 충돌, 이전 작업 차단, 작업 만료
- 추출 도우미: 실제 20초 제한을 포함한 모의 제공자 검사
- 생성 예약: 27개 검사, 동시 클릭·완료 재요청·불확실한 전송·한도 조회 실패
- 로컬 PostgreSQL: 105개 검사, 진단 작업 중 계정 이전 차단, 완료 자료 이전, 중복 이전, 오래된 작성자, RLS
- 브라우저: 세 진입 유형 완주, 새로고침, 두 탭 충돌, 서버 저장 직후 응답 유실, API 키 없이 메모 저장 후 진단 계속
- 화면: 320 / 390 / 768 / 1440px, 가로 넘침 검사와 스크린샷 확인

브라우저 측정은 클릭 후 질문 DOM 전환 시간이며 네트워크 왕복이나 실제 디스플레이 페인트 시간은 아니다. 최종 측정 30개 답변은 약 3~21ms였고 저장 응답을 450ms 늦춘 경우도 300ms 목표 이내였다. 저장 전에는 저장 완료로 표시하지 않는다. 외부 글꼴만 로컬 대체했으며 진단 API는 실제 로컬 서버에 요청했다. 최종 브라우저 보고서는 `artifacts/business-intake/1789526253089/report.json`이다.

테스트 결과는 `artifacts/business-intake/`에 기록한다. 로컬 미리보기는 API 키와 결제 설정을 전달하지 않는 `demo-memory` 환경이므로 서버 재시작 후 영구 보관을 보장하는 환경은 아니다. 영구 계정 이전 검사는 별도 로컬 PostgreSQL에서 수행했다.

## 공개 전 게이트

1. 변경 코드와 Workflow를 테스트 서버에 함께 배포하고 테스트 DB에 `0036_intake_account_claim.sql` 적용
2. 테스트 빌드에서만 `NEXT_PUBLIC_BUSINESS_INTAKE_V2=1` 설정
3. 실제 로그인 자료 이전, 원격 Workflow, 중단/재접속, 기존 결과물 연결 확인
4. 기존 비용 장부의 잔액과 실행별 상한을 재확인한 후 실제 AI 추출·도움·명시적 생성 검증
5. 운영 노출 후 `[business-intake]` 완료율과 `[business-intake-job]` 대기/실행 시간·오류를 기존 `[llm]` 실제 호출 로그와 구분해 관찰

이번 변경으로 운영 서버가 이미 전환됐거나 실제 AI 품질 검증까지 끝났다고 판정하지 않는다. 롤백 시 진행 중 자료를 삭제하지 않고 플래그만 되돌린다. 계정 이전 이후에는 기존 소유권 보호 기능을 끄지 않는다.
