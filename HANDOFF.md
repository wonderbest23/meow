# 오늘창업 인수인계

최종 정리: 2026-09-16 (KST). 다음 GPT/개발자는 이 문서를 먼저 읽고 실제 Git 상태와 최신 요청을 확인한다. 이 문서는 현황 기록이며 새 배포나 유료 호출을 승인하는 문서가 아니다.

> 후속 작업 안내: 아래 기본 현황은 Claude 작업 이전 기록이다. 이후 선택형 질문 등의 미커밋 변경과 통합 채팅 0~1단계 구현이 추가됐다. [Claude 작업 정리](docs/작업정리-2026-09-16-GPT-전달용.md) 및 [범용 사업 진단과 통합 채팅 계획의 10절](docs/intake-unified-business-plan-2026-09-16.md#10-01단계-구현-결과)을 먼저 읽는다. 과거 두 경로 분리/입력창 접힘 기획과 혼동하지 않고 기존 변경을 보존한다.

### 최신 후속 작업: 통합 채팅 0~1단계

- 2026-09-16 사용자 요청에 따라 현재 변경을 커밋/푸시 대상으로 정리했다. 헤더·모바일 요약 토글 변경까지 포함해 타입 검사, 70/70 회귀, 브라우저 재검사를 통과했다. 푸시 직전 최신 증거는 `artifacts/business-intake/1789559716400/report.json`이다. 커밋/배포의 실제 상태는 `git log`와 GitHub Actions에서 확인하며 아래의 과거 미커밋 기록과 구분한다. 공개 플래그와 DB 설정은 변경하지 않았다.
- 하단 입력창을 하나로 통합했다. 선택/직접 답변은 즉시 저장, 상담 질문·복합 문장은 원문으로 보관하거나 스레드에서 의도를 확인한다. 상시 입력 모드 탭은 제거했다.
- `note` API는 AI를 예약하지 않는다. 자동 메모 추출 효과를 제거했고 명시적인 버튼 요청만 기존 추출/진단 안내로 연결한다. 질문을 확정 사업정보로 사용하지 않는다.
- 빈 500/HTML/JSON 파싱 오류를 안전하게 처리하며 미전송 입력과 동일 요청 재시도 키를 유지한다. help 답변도 대화에 보관하되 문서 근거 버전은 유지한다.
- 최신 격리 미리보기는 61726이다. 8103은 오래된 결제 미리보기로 영구 저장소 없는 운영 모드 오류를 반환했다. 안전장치를 끄지 않았고 두 서버 모두 재시작하지 않았다.
- TypeScript 및 70/70 회귀 검사 통과. 서비스 모의 검사 26개, 최종 브라우저 세 유형 완주/충돌/응답 유실/빈 500 복구/320~1440px 통과. 최신 보고서 `artifacts/business-intake/1789556981505/report.json`.
- 이번 유료 호출·DB 변경·커밋/푸시·배포 없음. 기본 진단의 로컬 검증이며 범용 사업 엔진 전체 출시 완료가 아니다.
- 다음은 공통 사업 구조와 미분류·복합 사업 질문 경로다. 일반 사업 상담/원자적 운영 비용 예약/결과물 구조 연동/원격 검증은 남아 있다. 기존 `helpIntake`를 완성된 일반 상담 엔진으로 설명하지 않는다.

## 1. 작업 위치와 현재 배포

| 항목 | 확인한 값 |
| --- | --- |
| 실제 작업 저장소 | `/Users/juhong/Developer/meow-main` |
| 혼동 주의 | `/Users/juhong/Desktop/meow-main`도 존재하지만 최근 작업 저장소는 Developer 쪽이다 |
| 브랜치 / 원격 | `main` / `https://github.com/wonderbest23/meow.git` |
| 최근 커밋 | `0021a59c9ea49462d9a3b70e612f55d43632b263` |
| 커밋 제목 | `feat: add rules-first business intake and product editing safeguards` |
| 푸시 | `origin/main` 반영 완료 |
| 자동 배포 | [GitHub Actions 35055504144](https://github.com/wonderbest23/meow/actions/runs/35055504144) 성공, 2026-09-16 13:28 KST 완료 |
| 운영 도메인 | `https://oneulstart.com` |

`main`에 푸시하면 `.github/workflows/deploy.yml`이 회귀 검사 후 Cloudflare 배포를 실행한다. 따라서 단순 백업 목적으로 무심코 main에 푸시하지 않는다. 이번 인수인계 문서 자체는 아직 커밋/푸시하지 않았다.

배포 성공은 새 진단 흐름의 운영 공개나 원격 DB/결제 검증 완료를 뜻하지 않는다. 새 흐름은 빌드 시 `NEXT_PUBLIC_BUSINESS_INTAKE_V2=1`이어야 열리며 기본값과 예제 환경은 `0`이다. 현재 워크플로에는 이 값을 켜는 설정이 없다. 원격 공개 여부는 배포 환경과 실제 화면을 별도로 확인한다.

인수인계 시작 시 남아 있던 변경은 `.DS_Store`, `next-env.d.ts`다. 이전 커밋에서 의도적으로 제외했으며 되돌리거나 함께 커밋하지 않는다. 이후 추가 변경도 사용자 작업일 수 있으므로 보존한다.

## 2. 제품 방향: 반드시 유지할 경계

- 기본 질문 선택, 답변 저장, 분기, 기본 요약, 계산은 AI 호출 0회다. 버튼을 누를 때마다 AI가 대답하던 구조로 되돌리지 않는다.
- 아이디어 없음 / 생각한 사업 있음 / 운영 중인 사업을 구분한다. 각각 핵심 질문 11개, 업종별 선택 상세 질문 최대 4개다. 받은 정보와 미정으로 답한 항목을 반복 질문하지 않는다.
- 설문 페이지가 아니라 기존 채팅 안에서 질문과 답변이 이어지는 경험이어야 한다. 하단 입력창은 처음부터 유지한다.
- 질문에 대한 직접 답변과 자유 메모, 명시적 `AI 도움`을 구분한다. 맞춤 사업안/문서 생성은 사용자가 요청한 뒤에만 한다.
- 자유 메모의 원문을 먼저 저장한다. AI 추출 후보는 원문 근거와 함께 확인받기 전까지 확정 사업정보나 문서 근거를 덮어쓰지 않는다.
- AI 장애가 나도 기본 진단, 입력 수정, 저장, 기존 결과물 열기는 계속된다. 저장과 자동 해석의 완료 상태를 혼동하지 않는다.
- 문서/PPT/홈페이지의 수동 편집은 보존한다. 공통 사업정보 변경은 변경안 확인 후 반영하며 공개 홈페이지를 자동으로 바꾸지 않는다.

## 3. 가장 최근에 수정한 채팅 UX

1. 기존 환영 화면과 세 시작 선택지, 고정 하단 입력창, 대화 말풍선을 복원했다.
2. 첫 사업 설명을 직접 입력하면 대화 시작과 사업 내용 저장을 한 번의 버전 검증 요청으로 처리한다. 시작 버튼 선택을 강제하지 않는다.
3. 첫 입력 이후 전송이 잠기던 `introMessage` 조건을 수정했다. Enter 전송, Shift+Enter 줄바꿈, 한글 조합 중 전송 방지를 적용했다.
4. 사용자 답변을 즉시 표시하고 점 세 개의 `다음 질문 준비 중`을 거쳐 다음 질문을 표시한다. 최소 600ms는 화면 연출이며 서버 저장을 늦추거나 AI를 호출하지 않는다. 모션 감소 설정에서는 생략한다.
5. 스크롤 기준을 마지막 사용자 답변으로 바꿔 다음 질문에 가리지 않게 했다. 과거 대화를 읽는 사용자를 강제로 끌어내리지 않는다. 오류/충돌/화면 전환 시 이전 타이머를 취소한다.
6. 현재 질문의 뒤로가기 화살표를 없애고 `아직 미정`을 명확한 보조 버튼으로 만들었다. 이전 답변 목록은 사업 요약으로 이동했고 말풍선의 수정은 유지한다.
7. 업종은 사업 설명에서 규칙 기반으로 추천한다. 예: `50만원으로 SNS컨설팅`은 `기업 서비스`. `이 업종으로 계속`을 눌러야 확정하며 `직접 선택하기`에서 전체 업종을 펼친다.

업종 추천은 확인된 사업 소개/상품 입력만 근거로 사용한다. 미확정 AI 제안/자유 메모는 제외한다. 단서 부족, 부정 표현, 복합 사업, 상충 입력은 수동 선택으로 남긴다. 이미 수동으로 선택한 업종은 재접속/수정 시 추천으로 덮어쓰지 않는다. 의미를 완벽하게 이해하는 분류기로 설명하면 안 된다.

## 4. 주요 코드 위치

아래 경로는 저장소 루트 기준이다.

| 경로 | 역할 |
| --- | --- |
| `app/plan/chat/page.tsx` | 새 진단 기능 플래그와 진입 |
| `app/plan/chat/BusinessIntake.tsx` | 저장/복구, 소유자/버전 검증, 입력창, 낙관적 말풍선, 스크롤 |
| `app/plan/chat/intake-ui/IntakePanels.tsx` | 질문, 업종 추천/직접 선택, 요약, 추출 후보 확인 |
| `app/plan/chat/intake-ui/model.ts` | 입력 모델, 시작/선택지 해석, `suggestedIntakeIndustry` |
| `app/plan/chat/intake-ui/use-reply-transition.ts` | 질문 전환 최소 간격, 취소, 모션 감소 |
| `app/plan/chat/intake.module.css` | 진단 채팅 스타일 |
| `lib/plan-builder/intake-*.ts` | 질문팩, 규칙 엔진, 서비스/API, 비동기 추출 |
| `app/api/plan/chat/route.ts` | 기존 API와 새 명령 처리 |
| `lib/plan-builder/business-source-projection.ts` | 대화 상태와 실제 사업 근거의 버전 분리 |
| `supabase/migrations/0036_intake_account_claim.sql` | 진단 자료의 계정 이전 보호 |

새 진단 요청은 `/api/plan/chat`의 `x-business-intake: 2` 및 소유자 헤더를 사용한다. 중복 요청 영수증과 버전 검증을 우회하지 않는다. 저장 실패/409에서는 입력을 보존하고 비교 후 재시도한다.

AI 추출은 사업별 동시 1작업, 작업당 외부 호출 1회, 20초, 최대 1,200 출력 토큰이다. 자동 재시도/다른 제공자 재호출은 없다. 후속 메모는 다음 작업으로 묶는다. 늦은 응답은 시작 버전과 항목별 수정 여부를 확인하고 재확인 대상으로 남긴다.

## 5. 현재 미리보기 서버: 운영과 다름

- 주소: `http://127.0.0.1:61726/plan/chat?new=1`
- 실행 사본: `/private/tmp/oneul-intake-preview-0k0UM5`
- 매니페스트: `/private/tmp/oneul-intake-preview-0k0UM5/preview.json`
- `synthetic: true`, `paidApisEnabled: false`, 저장 방식 `demo-memory`, 새 진단 플래그 활성화.
- 저장소를 복사한 스냅샷이므로 Developer 저장소 수정만으로 이 서버가 자동 갱신되지는 않는다. 필요한 소스만 동기화하거나 안전한 미리보기를 다시 만든다.
- 사용자가 여기서 테스트 중이다. 서버를 재시작하면 메모리 자료가 사라질 수 있으므로 동의 없이 재시작하지 않는다. 다른 개발 서버도 일괄 종료하지 않는다.
- `.env.local`, 실제 API 키, PG 설정을 이 격리 미리보기에 복사하지 않는다. 이 서버에서 새로고침 성공을 영구 DB/로그인 복원 검증으로 해석하지 않는다.

최근 캐시 갱신은 `app/release-20260916.css`와 `app/layout.tsx`의 import 변경이다. `--oneul-release`는 `2026-09-16-intake-1`. 사용자 브라우저 저장소를 지우거나 Cloudflare 전체 캐시를 별도로 삭제하지 않았다.

## 6. 확인된 테스트와 한계

최근 구현 후 TypeScript 검사, UI 단위 검사, 전체 신뢰성 회귀 70/70 묶음을 통과했다. 이 문서 작성 중에는 테스트를 다시 실행하지 않았으며 CI의 동일 커밋 검사/배포 성공을 조회했다.

최신 브라우저 보고서: `artifacts/business-intake/1789531580929/report.json`.

- 320/390/526/768/1440px 화면, 세 진입 유형 완주, 첫 입력 전송, IME, 답변 수정, 질문/답변 동시 노출.
- 다중 탭 409 충돌 시 입력 보존, 저장 직후 응답 유실 후 재접속/같은 요청 재전송의 중복 방지.
- API 키 없는 상태에서 기본 진단 진행, 모션 감소, 업종 추천 확인/수동 변경/재접속/복합 사업 fallback.
- 보고서상 브라우저 오류와 차단된 외부 요청 없음, 유료 호출 0회.
- 31개 답변의 사용자 말풍선 DOM 반영 약 9~20ms, 다음 질문 약 606~629ms. 후자는 의도한 연출 간격이다. DOM 측정이지 전체 네트워크/화면 페인트 측정은 아니다.

핵심 엔진은 11업종 x 3진입의 33조합을 포함해 검사했다. 키 없음, 잔액 부족, 429, 잘못된 JSON, 지연 응답 등은 모의 장애 검사다. 원격 AI 품질/Google 로그인/실제 PG를 모두 통과했다는 뜻이 아니다. `artifacts/`는 Git 제외 대상이라 다른 머신에서는 별도 전달이 필요할 수 있다.

```sh
cd /Users/juhong/Developer/meow-main
git status --short --branch
npx tsc --noEmit --incremental false
node --import tsx scripts/business-intake-ui.test.tsx
node --import tsx scripts/reliability-tests.mts
```

격리 서버가 같은 경로로 살아 있을 때 브라우저 재검사:

```sh
INTAKE_PREVIEW_MANIFEST=/private/tmp/oneul-intake-preview-0k0UM5/preview.json \
RUNTIME_NODE_MODULES=/Users/juhong/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules \
node --import tsx scripts/business-intake-browser.mts
```

실제 AI 실행 스크립트와 위 무과금 테스트를 혼동하지 않는다.

## 7. 문서/PPT/홈페이지 상품화 현황

기존 작업은 버리지 않고 이어간다. `0021a59`에는 진단 외에 다음 보완도 포함된다.

- 홈페이지 요소별 비동기 갱신, 편집 세션/충돌 보호, 실패 복구, 이미지 자르기, 사업정보 가져오기, 문의 알림 outbox.
- 독립 결과물 변경 작업과 안정적인 항목 ID, 문서 최대 3항목/PPT 최대 4장 체크포인트, 명시적 승인과 버전 검증.
- 한 장 요약 PDF/Word, PPT v3 사본 변환, 페이지/요소 편집과 편집 가능한 PPTX 객체, 기간별 실적 근거 연결.
- 관련 마이그레이션 `0032`, `0034`, `0035`, `0036`. 파일 커밋과 원격 DB 적용은 별개다. 원격 적용 이력을 먼저 확인한다.

검증 기록에는 55개 업종/목적 조합을 포함한 67개 PPTX/696페이지의 LibreOffice/Poppler 렌더링, 165개 요약 조합, 홈페이지 80개 조합이 있다. 실제 PowerPoint/Google Slides 사용자 검증이나 55건 실제 AI 생성은 아니다.

실제 AI B2B 1개 사례는 문서 핵심 3항목과 PPT 12장을 생성하고, 수동 편집/가격 변경/승인형 갱신/로컬 JSON 복원/파일 내보내기까지 확인했다. 전체 상세 사업계획서나 원격 로그인 후 재접속 완주는 아니다. 근거는 `artifacts/industry-ai-20260915/b2b_service`, 판정은 `actual-ai-chain-complete-not-release-approved`다.

## 8. 남은 일과 다음 진행 순서

1. 최신 사용자 피드백과 현재 UI를 확인한다. 기본 진단 AI 0회 및 채팅 UX를 유지하며 실제로 남은 문제만 수정한다.
2. 테스트 서버에서 새 진단 플래그를 켜기 전에 DB 마이그레이션 적용 이력, API/Workflow 배포 버전, 계정 이전 구조를 확인한다. 승인 범위를 확인한 뒤 필요한 변경만 적용한다.
3. 비회원 자료 -> Google 로그인 -> 같은 사업 복원, 다중 탭, 중단/재접속, 명시적 생성, 기존 결과물 승인형 갱신을 원격 환경에서 검증한다.
4. PG 테스트 승인/권한 반영/실패 복구, Workflow 완료 후 재접속 다운로드를 실제 연결로 확인한다. 모의 결과 화면이나 로컬 테스트를 대체 증거로 쓰지 않는다.
5. 예산 재확인 후 아직 남은 실제 AI 업종 검증 범위를 정한다. 나머지 10개 업종, 전체 상세 계획서, 운영 중 사업, 계정/브라우저 재접속까지는 완료로 표시하지 않는다.
6. 실제 Storage 업로드/취소 파일 정리, 홈페이지 공개/재공개/복원/캐시, 실제 소유자 이메일 수신과 재시도 스케줄 실행을 확인한다. outbox 구현만으로 이메일 운영 완료가 아니다.
7. PowerPoint/Google Slides 대표 판형과 사용자 5명 중 4명 핵심 작업 성공, 업종별 품질 평가를 확인한 뒤 공개 여부를 판단한다.

사용자 소유 테스트 앱은 이전 정정상 `newapp`이다. 프로젝트 이름만으로 대상 DB를 단정하지 말고 실제 프로젝트 ID/배포 설정을 재확인한다. 외부 테스트 리소스 생성 승인이 있었지만 새 유료 플랜, 실제 결제, 무제한 API 호출을 허용한 것은 아니다.

현재는 구현과 로컬 검증이 크게 진행된 상태이지 전체 업종 판매 준비가 최종 승인된 상태가 아니다. 새 진단 공개 플래그를 여는 작업과 새 main 푸시는 운영에 영향을 준다. 사용자의 현 요청이 인수인계뿐이면 추가 배포하지 않는다.

## 9. API 예산과 보안

2026-09-16 `artifacts/synthetic-ai-launch/budget.json`을 읽기 전용으로 재확인했다.

| 항목 | 값 |
| --- | --- |
| 누적 승인 상한 | $30 |
| 누적 요청 | 21건 (완료 20, HTTP 실패 1) |
| 보수적 누적 예약액 | $18.99255 |
| 남은 예약 한도 | $11.00745 |

이는 실제 청구액 확정이나 새 예산이 아니다. 실패 요청 예약을 임의 환급하거나 장부를 초기화하지 않는다. 다음 업종 전체 검증의 기존 보수적 상한은 남은 한도에 맞지 않는 것으로 기록돼 있다. 실행 직전 최신 장부와 작업별 상한을 다시 계산하고 부족하면 멈추고 추가 승인을 받는다. 출처 해시/잠금/예산 보호를 우회하거나 이미 완료된 유료 작업을 무심코 재실행하지 않는다.

2026-09-17 추가 기록: 소유자 요청으로 구조 브리프 반영 확인 1건(`scripts/structure-brief-live.mts --live --approved-total-usd=30`, 가상 구독 카페·문서 항목 1개·2회 호출)을 실행했다. 예약 $2.95355가 더해져 **누적 23건, 보수적 누적 예약액 $21.9461, 남은 예약 한도 $8.0539**이다. 위 표는 09-16 시점 값이다.

예산 코드: `scripts/synthetic-ai-budget.ts`. 실행/복구 절차: `docs/industry-ai-live-20260915.md`, `scripts/industry-ai-live.mts`. 장부와 증거 파일은 이 머신의 로컬 파일일 수 있다.

API 키가 과거 대화에 노출된 이력이 있다. 키 자체를 문서/로그/커밋에 복사하지 말고 교체가 필요하다. `.env.local` 전체를 출력하거나 다른 환경으로 복사하지 않는다. 사용자 사업정보로 유료 테스트하지 않고 가상 사업 자료를 사용한다.

## 10. 이어 읽을 문서

- [진단 구현과 채팅 UX 상세](docs/business-intake-implementation-2026-09-16.md)
- [결과물 상품화 구현/검증/미완료 범위](docs/productization-implementation-2026-09-15.md)
- [이전 연동 보완 기록](docs/live-handoff-fixes-2026-09-15.md)
- [실제 AI 검증과 복구 절차](docs/industry-ai-live-20260915.md)
- [제품 준비 상태](docs/product-readiness-2026-09-13.md)
- [출시 준비 상태](docs/release-readiness-2026-09-14.md)

위 과거 문서의 "미커밋/미배포"는 작성 당시 상태다. 최신 소스는 `0021a59`로 커밋/푸시 및 자동 배포 성공했다. 단, 그 문서에 적힌 미검증 범위가 배포만으로 해결된 것은 아니다. 서로 다른 날짜의 증거를 합쳐 "전부 검증 완료"로 요약하지 않는다.
